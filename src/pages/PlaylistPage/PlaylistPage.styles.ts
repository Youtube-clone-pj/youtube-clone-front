import styled from "styled-components";

export const PlaylistPageLayout = styled.div`
	padding-top: 60px;
	padding-left: 256px;
	background-color: ${({ theme: { colors } }) => colors.black_primary};
`;
