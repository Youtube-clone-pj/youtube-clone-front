import styled from "styled-components";

export const VideoPrimaryLayout = styled.div`
	flex: 1;
	flex-basis: 1e-9px;
	margin-left: 24px;
	max-width: calc((100vh - 216px) * (16 / 9));
	min-width: calc(360px * (16 / 9));
	padding: 24px 24px 0 0;
`;

export const VideoPrimaryDiv = styled.div``;
